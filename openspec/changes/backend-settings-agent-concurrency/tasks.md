## 1. 配置模型与兼容迁移

- [x] 1.1 引入兼容 Pydantic v2 的 `pydantic-settings` 依赖，并将现有环境变量、`.env`、默认值和脱敏状态迁移到统一 Settings 模型。
- [x] 1.2 增加 `MEMEMEOW_OPENCODE_CONCURRENCY` 与设置管理凭据配置，校验并发范围 `1..8`，保持进程环境优先级和未知变量兼容。
- [x] 1.3 为 dotenv 路径、空值、布尔/整数解析、环境覆盖、敏感字段 repr 和启动错误补充后端单元测试。

## 2. 后端设置 API 与持久化

- [x] 2.1 实现脱敏的后端设置查询响应，返回三类字段、有效值、待生效值、配置版本和 `restart_required`，不返回密钥或原始日志。
- [x] 2.2 实现只允许更新并发数量的受保护接口；未配置或凭据错误时返回 `403`，环境变量覆盖时拒绝写 `.env`。
- [x] 2.3 实现受控 `.env` 单字段原子更新，保留未知变量和注释，拒绝符号链接/越界路径并确保文件权限安全。
- [x] 2.4 将非敏感 settings version/并发摘要接入任务创建和任务详情，保持旧任务历史和状态转换兼容。

## 3. 后端设置前端页面

- [x] 3.1 在现有页面导航中新增独立“后端设置”入口，不与搜索、上传、任务页的瞬时前端偏好混用。
- [x] 3.2 实现三类配置展示：只读后端状态、安全可调整的 Agent 并发数量、部署环境管理的高风险参数。
- [x] 3.3 为并发数量提供范围校验、保存/失败状态、环境覆盖提示、当前值与待生效值对照，以及明确的重启提示。
- [x] 3.4 确保 API key、设置管理凭据、完整路径敏感信息不进入表单初始值、localStorage、页面日志或错误提示。
- [x] 3.5 补充桌面/移动端页面测试、只读降级、未授权更新、配置加载错误和重启提示测试。

## 4. 联调与验收

- [x] 4.1 将有效并发设置接入并行 Agent lane，验证默认 `1`、保存后重启生效以及环境变量覆盖行为。
- [x] 4.2 完成设置 API、任务查询、并行提取和缓存协调的端到端测试，确认新旧配置不会混用运行中的任务。
- [x] 4.3 更新 `.env.example`、API 文档、任务页/后端设置页文案，并执行完整测试、严格 OpenSpec 校验和前端构建。
