## 1. 基础边界与配置

- [ ] 1.1 建立 FastAPI 应用生命周期和服务依赖注入，确保搜索、VLM 与缓存服务按进程初始化一次
- [ ] 1.2 增加 `.env.example` 和服务端配置加载，覆盖嵌入模型、VLM、数据根目录和 API 策略
- [ ] 1.3 增加统一请求校验、错误响应 schema 和 OpenAPI 元数据
- [ ] 1.4 增加配置状态接口，确认密钥只返回脱敏状态且不存在前端写配置路径
- [ ] 1.5 增加受控文件标识解析器，覆盖规范化路径、根目录边界、符号链接和文件名校验

## 2. 任务与缓存

- [ ] 2.1 实现内存任务管理器，支持 `queued`、`running`、`succeeded`、`failed` 状态和线程安全查询
- [ ] 2.2 实现任务提交与 `GET /tasks/{task_id}` 状态接口，覆盖未知任务和失败错误响应
- [ ] 2.3 实现服务关闭时将未完成任务标记为失败，并验证重启后不恢复旧任务
- [ ] 2.4 将缓存生成接入任务管理器，增加同类任务互斥和已有缓存刷新期间继续使用旧缓存的逻辑
- [ ] 2.5 为缓存不存在、生成中、生成成功、生成失败补充后端契约测试

## 3. 检索 API

- [ ] 3.1 重构 `POST /search` 请求模型，统一 `query`、`n_results` 和 `llm_enhance` 字段及 1 到 30 的范围校验
- [ ] 3.2 将检索结果映射为受控媒体 URL，移除绝对路径和重复的路径转换逻辑
- [ ] 3.3 固定相关性排序、稳定次级排序、重复图片去重和不可访问图片跳过行为
- [ ] 3.4 实现 LLM 增强失败时回退原始查询的逻辑
- [ ] 3.5 移除或拒绝旧的 GET `/search` 语义入口，并更新 `api.md`、README 和 OpenAPI 示例
- [ ] 3.6 增加检索契约测试，覆盖参数错误、缓存未就绪、正常结果、空结果和 LLM 回退

## 4. 图片库与媒体

- [ ] 4.1 实现图片目录列表和分页/筛选所需的稳定图片元数据响应
- [ ] 4.2 实现受控媒体接口，验证内容类型、文件存在性和路径边界
- [ ] 4.3 实现创建图片目录接口，覆盖非法名称、重复目录和并发冲突
- [ ] 4.4 实现图片重命名接口，覆盖扩展名保留、非法字符清理、目标冲突和不覆盖原则
- [ ] 4.5 增加目录穿越、符号链接逃逸和媒体越界访问的安全测试

## 5. 上传与标注

- [ ] 5.1 实现多文件上传接口，校验 PNG/JPG/JPEG/GIF 内容和大小限制
- [ ] 5.2 实现逐文件上传结果，确保批量部分失败不回滚已成功文件且不覆盖重复文件
- [ ] 5.3 接入可选 VLM 自动命名；失败时回退原始安全文件名并返回单文件状态
- [ ] 5.4 实现单张 VLM 描述接口，提供候选描述、失败状态和有限重试
- [ ] 5.5 实现人工命名提交和相邻图片/筛选列表导航所需的 API 行为
- [ ] 5.6 将批量标注和预生成接入长任务，确保预生成失败不影响当前图片操作
- [ ] 5.7 增加上传、描述生成、重命名冲突和预生成失败的契约测试

## 6. Vue 前端

- [ ] 6.1 创建 Vue 3 前端工程和统一 API 客户端，集中处理错误响应与媒体 URL
- [ ] 6.2 实现检索页面，包含缓存状态、查询、结果数量、LLM 开关、空结果和错误状态
- [ ] 6.3 实现图片库页面，包含目录选择、列表筛选、分页、预览、创建目录和重命名
- [ ] 6.4 实现上传页面，包含多文件选择、自动命名开关、逐文件进度和部分失败提示
- [ ] 6.5 实现标注页面，包含 VLM 候选、人工命名、上一张/下一张、跳转和任务轮询
- [ ] 6.6 为缓存生成和批量标注实现任务状态轮询，处理成功、失败和服务重启后的不可恢复状态
- [ ] 6.7 增加 Vue 端核心流程的组件测试或端到端测试

## 7. 下线旧功能与交付

- [ ] 7.1 在新 API 和 Vue 流程验收通过后停用 Streamlit 生产入口
- [ ] 7.2 删除资源包管理、社区同步和资源包导出路由、页面入口及不再使用的依赖引用
- [ ] 7.3 清理检索服务对资源包管理器的运行时依赖，并验证历史缓存格式的拒绝或迁移行为
- [ ] 7.4 更新 Docker、启动命令、部署说明和 `.gitignore`，确保 `.env` 不被提交
- [ ] 7.5 执行完整后端契约测试、前端端到端测试和目录越界安全测试
- [ ] 7.6 运行 OpenSpec 严格校验，确认实现任务逐项对应规格场景
