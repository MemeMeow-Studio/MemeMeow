## 实现任务

- [x] 扩展请求校验和 fingerprint，保持 `units` 正整数。
- [x] 扩展 operation grant ORM 与 Alembic migration，并兼容历史行。
- [x] 扩展内存/持久 acquire、commit、release、recovery 请求事实传递。
- [x] 增加 `0`、`1000`、`2000`、fingerprint 差异和生命周期测试。
