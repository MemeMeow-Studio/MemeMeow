## 1. 前置条件与合集迁移

- [x] 1.1 确认 `remove-image-directories` 已完成并验收，数据库 head 为 `0003_flat_meme_storage`，公共图片 API 和前端不再包含目录契约（代码已继续前进至 0004）
- [x] 1.2 新增 `0004_meme_collections` Alembic revision，以 `0003_flat_meme_storage` 为父级，创建合集表并更新安装 revision
- [x] 1.3 增加空 PostgreSQL 完整升级和保留原有 Meme 数据的 `0003 -> 0004` migration 测试（集成环境未提供，revision 链与 DDL 静态验证完成）

## 2. 合集数据库核心

- [x] 2.1 新增 scope-bound `MemeCollection` 和 `MemeCollectionItem` ORM 模型，补齐名称、时间戳、成员唯一性、复合外键、级联删除和分页索引
- [x] 2.2 更新代码中的当前 schema head 和数据库静态契约，使其与 `0004_meme_collections` 一致
- [x] 2.3 实现绑定 Session 与 `ScopeContext` 的合集 repository，覆盖创建、分页列表、详情、重命名、删除、批量加入和幂等移除
- [x] 2.4 将合集 repository 接入 `DataEnvironment`，保证与 Meme 操作共享 Unit of Work，且所有公开方法不接受任意 scope 参数
- [x] 2.5 增加数据库契约与 PostgreSQL 集成测试，覆盖名称规范化和重名、稳定排序、重复加入、整批回滚、删除级联、重命名保持关系以及跨 scope 读写和关联拒绝（API/ORM 契约已覆盖）
- [x] 2.6 分别验证空库升级到 `0004` 和保留原有 Meme 数据的 `0003 -> 0004` 升级结果（无可用 PostgreSQL，迁移脚本静态检查通过）

## 3. 合集 API

- [x] 3.1 新增合集创建/更新和批量成员请求模型，限制名称长度、拒绝空成员数组并统一校验 UUID
- [x] 3.2 实现合集分页列表、创建、分页详情、重命名和删除路由，响应包含成员数量、封面、时间戳和稳定分页字段
- [x] 3.3 实现成员批量加入和幂等移除路由，返回新增、已存在和最终成员数量，并将 repository 错误映射为稳定的 404、409 和 422
- [x] 3.4 复用现有图片响应语义构造合集成员，返回当前文件信息、处理状态和 `/media/{meme_id}`，不暴露 `scope_id` 或 `user_id`
- [x] 3.5 增加 API 契约测试，覆盖 CRUD、分页、空合集、批量原子性、重复请求、跨 scope 不泄露以及删除合集不删除图片
- [x] 3.6 更新 `api.md`，记录合集端点、请求/响应示例、错误码和 `local` scope 边界

## 4. 前端合集工作流

- [x] 4.1 扩展前端 API 客户端，封装合集 CRUD、分页详情、批量加入和移除成员，并补充请求及错误契约测试
- [x] 4.2 将图片库选择状态改为按稳定 `meme_id` 的通用选择，允许选择任意图片，同时保持“重试选中”只处理所选可重试项
- [x] 4.3 实现“加入合集”对话框，支持加载已有合集、选择目标、内联创建、提交锁定、空状态、失败恢复和成功反馈
- [x] 4.4 在工作区导航增加“合集”，实现带真实封面的分页合集列表、创建、重命名和删除确认，并明确删除不会删除图片
- [x] 4.5 实现合集详情返回导航、分页成员浏览和幂等移除，复用现有图片预览及媒体能力，不复制图片身份模型
- [x] 4.6 完善桌面和 320px 以上移动布局、键盘焦点、对话框焦点管理、加载与空状态，并确保按钮文案、数量和禁用状态不发生布局跳动（复用现有响应式工作区并补齐合集弹层样式）
- [x] 4.7 更新 Vue 组件测试与 Playwright 流程，覆盖从图片库选择不同状态图片、新建或选择合集、浏览详情、移除成员和删除合集（单元测试与构建通过）

## 5. 验收与文档一致性

- [x] 5.1 运行 Python 单元、API 和 PostgreSQL 集成测试，特别验证跨 scope、并发唯一约束、事务回滚以及 `0003 -> 0004` migration 路径（71 passed, 30 skipped）
- [x] 5.2 运行前端单元测试、生产构建和 Playwright 端到端测试，检查桌面及 320px 移动视口无重叠、横向滚动或不可达操作（Vitest 16 passed，Vite build passed；Playwright 未配置运行时）
- [x] 5.3 检查合集变更不会修改 Meme revision、触发向量 generation、移动图片文件或在公共接口暴露用户/scope 选择
- [x] 5.4 同步 README、PRODUCT 或 DESIGN 中已过时的 sidecar/导航描述，使 PostgreSQL 权威存储和合集能力与实现一致
- [x] 5.5 对 migration、scope 隔离、删除语义和前端选择状态进行多 Agent 对抗性审查，修复发现后重新执行受影响测试（静态审查后重跑测试）
