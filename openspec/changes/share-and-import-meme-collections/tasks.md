## 1. 合集包格式与服务边界

- [x] 1.1 新增具有中文模块、类和函数 docstring 的合集包服务模块，定义 `mememeow-collection` v1 manifest 模型、ZIP 路径规则、500 张成员上限和 512 MiB 解压总大小上限。
- [x] 1.2 实现安全文件名、manifest 序列化、SHA-256 计算和同名文件冲突解析辅助函数，并覆盖 Unicode 名称、哈希后缀递增和非法业务文件名测试。
- [x] 1.3 实现 ZIP 中央目录和 manifest 的零副作用预检，拒绝缺失或重复条目、绝对路径、父目录穿越、NUL、符号链接、未声明文件、未知格式版本、大小或哈希不符以及不可解码图片。
- [x] 1.4 为 manifest 往返、空合集格式、损坏 ZIP、路径攻击、超出成员数量、超出单文件或总大小限制补充聚焦单元测试。

## 2. 动态合集导出

- [x] 2.1 扩展合集 repository，提供不分页的稳定成员导出查询，并排除存在 `prepared` 或 `file_applied` 存储操作的 Meme。
- [x] 2.2 实现临时 ZIP 生成流程，按合集成员顺序写入 `manifest.json` 和 `images/<meme_id>.<ext>`，逐文件复核大小与 SHA-256，并确保成功、异常和客户端响应结束后都清理临时文件。
- [x] 2.3 新增 `GET /collections/{collection_id}/export`，绑定固定 `local` scope，返回安全 `Content-Disposition` 的 `application/zip`，并将合集不存在、成员不可读取、指纹不一致和大小超限映射为稳定错误。
- [x] 2.4 增加 API 与 PostgreSQL 集成测试，覆盖空合集、正常合集、成员顺序、同一 URL 在成员变化后返回新内容、跨 scope 不可见、活动存储操作和缺失或不匹配文件不产生部分 ZIP。

## 3. 合集 ZIP 导入

- [x] 3.1 新增 `POST /collections/import` multipart 边界，严格只接受单个 ZIP 文件，并在创建合集前完成格式、路径、资源限制、SHA-256 和图片解码预检。
- [x] 3.2 实现合集名称冲突的零副作用返回，并为每个 manifest 成员解析“同名同 SHA 复用、同名不同 SHA 使用哈希后缀、新名称直接创建”的目标 Meme。
- [x] 3.3 对新图片复用现有 `StorageCoordinator` 上传和语境任务提交，对复用或新建的 Meme 建立合集成员关系，并按 manifest 顺序返回 `source_meme_id` 到目标 Meme ID 的逐项结果。
- [x] 3.4 在预检通过后的运行时失败中保留已成功图片和成员，明确返回部分成功及可重试的语境任务错误，并只在实际新增图片时失效搜索缓存。
- [x] 3.5 增加 API 与 PostgreSQL 集成测试，覆盖成功导入、合集重名、同名同内容复用、同名不同内容改名、不同文件名相同内容、部分存储失败、语境任务失败、重复包导入和来源 ID 不被直接复用。

## 4. 前端合集操作

- [x] 4.1 在合集详情增加使用下载图标的 ZIP 下载按钮和复制链接图标按钮，使用浏览器原生下载地址并为图标提供 tooltip 和可访问名称。
- [x] 4.2 在合集列表增加导入命令和独立 `.zip` 文件对话框，通过新增 multipart API 客户端提交包并显示进行、名称冲突、包校验失败、逐项部分成功和全部成功状态。
- [x] 4.3 导入成功后提供直接打开新合集的操作，并确保桌面和 320 像素以上移动视口没有文本溢出、横向滚动或控件遮挡。
- [x] 4.4 补充前端组件测试，覆盖下载 URL、剪贴板复制、文件类型限制、提交锁定、错误反馈、部分成功结果和成功跳转。

## 5. 契约、文档与验收

- [x] 5.1 更新 API 文档和 README 资源包说明，记录动态下载 URL、ZIP v1 结构、导入部分成功语义、文件冲突规则以及不包含认证和 embedding 的边界。
- [ ] 5.2 运行后端单元测试、API 测试、PostgreSQL 集成测试和前端测试，并修复全部回归。
- [x] 5.3 使用真实合集完成“下载、修改成员、再次下载、导入 ZIP、打开新合集”的端到端验收，并检查临时文件清理和移动端布局。
- [ ] 5.4 开启独立 Agent 对 ZIP 安全边界、动态读取语义、部分导入结果和回归测试覆盖做对抗性审查，并处理确认的问题。
