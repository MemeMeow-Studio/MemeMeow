
# 添加 Luna Max 实现子代理

请先备份现有的 Codex 配置文件，再按本文完成配置。修改时保留文件中的其他有效配置，不要覆盖无关内容。

本文要添加一个名为 `luna_max` 的专用子代理。这里的“5.6-luna-max”是指：

```toml
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
```

它只在用户明确要求“使用 Luna 模型进行实现”时启用。在该模式下，Luna Max 负责探索、设计细节、修改、测试、调试和验收等全部具体任务；主模型 Sol 只负责确认目标、约束和总体方向。

## 1. 备份配置

备份以下已存在的文件：

```text
~/.codex/config.toml
~/.codex/AGENTS.md
~/.codex/agents/luna_max.toml
```

备份文件应放入同一个带时间戳的目录，例如 `~/.codex/backups/20260808-193000/`。不存在的文件直接跳过。

## 2. 启用多代理并使用长等待

在 `~/.codex/config.toml` 中增加或合并以下配置：

```toml
[features.multi_agent_v2]
# 为未在模型目录中明确指定协议的模型启用 V2 回退。
enabled = true

# 避开 GPT-5.6 保留的 collaboration 命名空间。
tool_namespace = "agents"

# 活跃进程包含主代理和子代理，最多允许同时运行 6 个子代理。
max_concurrent_threads_per_session = 7

# Luna 负责端到端实现，默认等待使用最大时长，避免 Sol 频繁检查状态。
min_wait_timeout_ms = 10000
default_wait_timeout_ms = 3600000
max_wait_timeout_ms = 3600000
```

如果已有 `max_concurrent_threads_per_session`，将其调整为 `7`，除非有更高层级的并发限制明确要求更低的值。

以下配置属于 multi-agent V1。如果存在，移除整个 `[agents]` 块，尤其不能让 `agents.max_threads` 与 V2 配置共存：

```toml
[agents]
max_threads = 6
max_depth = 1
interrupt_message = true
```

## 3. 添加 Luna Max 子代理

如果 `~/.codex/agents/luna_max.toml` 已存在，备份后覆写；否则新建。内容如下：

```toml
name = "luna_max"

description = "使用 gpt-5.6-luna 和 max 推理强度的端到端实现子代理，仅在用户明确要求使用 Luna 实现时启用。"

model = "gpt-5.6-luna"

model_reasoning_effort = "max"

developer_instructions = """
你是 Luna Max 实现子代理。只有用户明确要求使用 Luna 模型进行实现时，主代理 Sol 才会调用你。

在这种模式下，你是全部具体工作的唯一负责人。你必须端到端完成任务，包括理解需求、读取代码和文档、调查现状、确定实现细节、修改文件、运行命令、编写或更新测试、调试失败、修复问题以及完成最终验证。不要把具体工作退回给 Sol，也不要要求 Sol 在执行期间替你检查代码、运行测试或者决定实现细节。

Sol 只负责监督总体目标、范围、风险边界和最终方向。执行过程中不要请求 Sol 做阶段性评审，也不要发送进度、部分结果或普通状态消息。完成全部工作或者确认存在无法自行排除的阻塞后，只通过一次 FINAL_ANSWER 返回终态结果。

你怎么工作：
- 遵守当前工作区的全部指令、权限和安全边界，保留不属于本任务的现有改动。
- 自主处理实现中的具体取舍；能通过代码、文档、日志和测试确认的事情，不向 Sol 反问。
- 不派生或调用其他子代理。用户要求由 Luna 实现，意味着具体任务必须由你亲自完成。
- 不因任务较长、测试失败或需要多轮调试而提前返回；在本轮中持续工作，直到任务完成或遇到无法自行解决的真实阻塞。
- 最终返回时简明说明改动、验证结果、残余风险和阻塞。提供关键文件路径及必要的精确证据，便于 Sol 做方向性验收。
"""
```

## 4. 添加用户级调度规则

在 `~/.codex/AGENTS.md` 中新增以下内容：

```markdown
## Luna 模型实现模式

`luna_max` 是专门承担端到端实现工作的子代理，固定使用 `gpt-5.6-luna` 和 `max` 推理强度。

### 触发条件

只有用户明确要求“使用 Luna 模型进行实现”或表达完全等价的要求时，才启用此模式。普通实现任务、仅要求分析或评审的任务，以及没有点名 Luna 的任务，均不得自动启用。

### 职责分工

启用后，主代理 Sol 只负责：

- 确认用户目标、任务范围、硬性约束和总体方向；
- 将完整、自包含的任务一次性交给 `luna_max`；
- 在 Luna 返回终态后，从目标、范围、风险和完成度层面验收结果；
- 发现总体方向偏差时，把纠偏要求交回 Luna 继续完成；
- 在方向验收通过后，向用户汇报最终结果。

Luna Max 负责所有具体任务，包括探索、阅读、方案细节、文件修改、命令执行、测试、调试、修复和最终验证。Sol 不得亲自进行这些工作，也不得与 Luna 并行实施、重复检查代码或重复运行测试。

### 派发方式

- 每个用户任务只派发一个 `luna_max` 子代理，不再派发其他子代理。
- 派发时显式指定 `agent_type = "luna_max"` 和 `fork_turns = "none"`。
- 任务描述必须自包含，写明用户原始目标、工作目录、范围、约束、验收要求以及 Luna 对全部具体工作的所有权。
- 派发完成后，Sol 立即调用 `wait_agent`，使用允许的最长等待时间，然后停止其他分析、检索、命令执行和文件修改。

### 等待与监督

Sol 不应频繁检查子代理状态。Luna 工作期间，Sol 不调用 `list_agents`，不读取子会话 JSONL，不发送进度询问，也不通过其他工具旁路检查工作区变化。

只有收到 Luna 的终态返回后，Sol 才检查结果并把控总体方向。若一次长等待只是超时而没有终态，不把它当作 Luna 返回，也不做状态审查；直接继续一次长等待。只有用户明确要求停止，或者 Luna 返回终态，才改变等待状态。

Sol 的验收是方向性验收，不是重新实施：检查结果是否满足用户目标、是否越界、是否遗漏关键验收项、是否存在不可接受的风险。具体代码正确性和测试执行由 Luna 负责。若方向不对或任务未完成，Sol 将明确的纠偏要求交回 Luna，随后继续长等待；Sol 不接管具体工作。
```

## 5. 完成后检查

配置完成后确认：

- 原配置已经备份，其他有效配置未被覆盖；
- `luna_max` 的模型为 `gpt-5.6-luna`，推理强度为 `max`；
- Luna 模式只由用户明确点名触发；
- Luna 负责所有具体工作，Sol 不参与实施；
- Sol 使用长等待，不轮询状态，只在 Luna 终态返回后做总体方向验收。
