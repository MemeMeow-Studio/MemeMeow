## 1. 规格与共享预检

- [x] 1.1 完成图片入库和合集导入的资源边界 delta spec，并通过 OpenSpec 严格校验
- [x] 1.2 新增共享图片预检模块、中文 docstring、常量和稳定错误码
- [x] 1.3 实现每帧 25M 像素、100 帧、累计 100M 像素及可终止 10 秒预检

## 2. 上传入口

- [x] 2.1 在直接上传幂等查找和 durable 写入前调用共享图片预检
- [x] 2.2 让上传超限/超时逐文件返回稳定错误，不启动视觉、文本或 Agent 处理
- [x] 2.3 补充无副作用、模型不调用和边界值测试

## 3. 合集 ZIP

- [x] 3.1 限制 ZIP 原始字节 64 MiB、成员 500、单成员 20 MiB、总解压 512 MiB 和 100:1 放大比
- [x] 3.2 拒绝 ZIP64、嵌套归档、链接、目录及特殊文件，并核对声明与实际大小
- [x] 3.3 让导入 multipart 有界读取，更新稳定错误映射
- [x] 3.4 导出使用 no-follow 文件描述符读取，并强化临时路径清理
- [x] 3.5 补齐恶意 ZIP、竞态替换和失败清理测试

## 4. 验证与交付

- [x] 4.1 运行相关 pytest、全量 pytest、compileall、diff check 和 OpenSpec strict validate
- [x] 4.2 对新增边界做安全/并发/副作用审查并修复 P1/P2
- [x] 4.3 只暂存本 change 和公共实现/测试文件，确认用户已有脏文件未进入 commit
- [x] 4.4 创建开源审核用本地 commit，报告精确 SHA 后停止，不同步到 Server
