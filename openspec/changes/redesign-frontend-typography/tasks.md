## 1. 基线与设计令牌

- [x] 1.1 读取当前 `frontend/src/App.vue`、`frontend/src/style.css`、前端测试与工作区 diff，记录并保留已有缓存动作、任务轮询和用户改动
- [x] 1.2 在 `frontend/src/style.css` 建立固定紫蓝 `#5157D9`、深墨 `#2A2638`、背景 `#F8F7FB`、功能状态色、字体层级、行高、间距、边框和焦点 token，并补充中文文件级/函数级说明
- [x] 1.3 清理互相覆盖的一次性样式，建立桌面侧栏、内容区、工作区标题、工具栏、状态标签和列表的稳定布局骨架

## 2. 工作区结构与状态

- [x] 2.1 重构 `App.vue` 的四区导航和当前工作区语义结构，保留既有标题/功能文案与轻微倾斜的 `M` 标记，保证桌面侧栏与移动四项导航共享稳定的当前态和键盘交互；上传入口只在主导航保留，侧栏不得重复提供“上传图片”主动作
- [x] 2.2 调整检索工作区的输入、提交、缓存未就绪、加载、空结果和错误反馈，明确恢复动作且不改变 API 调用
- [x] 2.3 调整图片库工作区的目录/图片浏览、预览、缓存生成入口、加载、空态和失败反馈，兼容现有缓存动作
- [x] 2.4 调整上传工作区的选择、自动命名开关、逐文件成功/失败结果和批量部分成功反馈
- [x] 2.5 调整处理任务列表与详情的字段分组、状态文案、进度、时间、错误恢复和未知状态回退，避免原始 enum 直接挤压布局

## 3. 排版、响应式与可访问性

- [x] 3.1 将正文与关键操作提升到规格要求的字号层级，功能元数据保持至少 14px；移除装饰性小字、eyebrow、随机图案和拟造图片字幕，确保真实文案在桌面/移动不截断
- [x] 3.2 实现 320px 及以上宽度的响应式规则，处理长文件名、任务消息、按钮组和缩略图的换行/截断，消除水平溢出
- [x] 3.3 为导航、按钮、输入、文件选择和任务详情入口补充语义标签、键盘焦点、禁用态和对比度；加入 `prefers-reduced-motion` 规则
- [x] 3.4 使用一致的图标来源或现有项目图标，移除以 Unicode/emoji 代替图标的新增用法，并为不熟悉的图标提供可访问名称

## 4. 验证与文档

- [x] 4.1 更新/补充 `frontend/src/App.test.js` 或现有端到端测试，覆盖四区导航、关键状态文案、缓存动作兼容和未知任务状态回退
- [x] 4.2 运行前端单测与 `npm run build`，修复由模板/CSS 重构引起的行为或构建回归
- [x] 4.3 用 Playwright 或项目既有浏览器流程截取桌面、移动和 320px 视口，检查字号层级、焦点、对比度、状态可扫描性和水平溢出；概念草图 v2 需重新截取 1440x900 与 390x844 截图，并验证图片加载、console/page errors 与 scrollWidth；按两轮有界检查修复问题
- [x] 4.4 更新 `DESIGN.md` 记录简洁 MemeMeow 工作台方向、固定主题色、克制品牌标记、颜色/排版 token、组件状态和响应式规则；将“最近看过”标记为后续独立需求，并运行 OpenSpec 校验确认变更完整
