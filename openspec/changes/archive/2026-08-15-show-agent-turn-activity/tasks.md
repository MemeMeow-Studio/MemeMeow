## 1. OpenCode 活跃度读取边界

- [x] 1.1 新增带中文模块、类和函数 docstring 的只读 OpenCode 活跃度适配器，定义领域结果并从 runtime 根目录定位 `opencode.db`
- [x] 1.2 使用只读、`query_only`、短 busy timeout 的 SQLite 连接批量匹配 `mememeow-task-{task_id}` 最新 session，统计 `step-finish`、未完成 step 及最大 part 更新时间
- [x] 1.3 将文件缺失、权限、锁、schema/JSON 和时间转换异常收敛为无数据，并确保 reader 不写库、不复制主库且不使用 `immutable=1`
- [x] 1.4 添加 reader 单元测试，覆盖完成轮次、进行中轮次、重复标题取最新 session、UTC 时间、空输入、无 session、WAL 可见性及各类降级路径

## 2. 任务 API 装配

- [x] 2.1 在应用初始化中注入活跃度 reader，并提供测试可替换的边界；reader 初始化失败不得阻止服务启动
- [x] 2.2 重构任务摘要装配，使列表对当前页语境研究任务执行一次批量读取，单任务接口复用同一逻辑，并仅在完整活动数据可用时返回三个可选字段
- [x] 2.3 添加 API 测试，覆盖列表和详情字段、非语境任务省略字段、无 session/reader 异常降级，以及批量列表不产生 N+1 查询
- [x] 2.4 更新 API 文档，说明字段类型、可选条件、UTC 时间语义及“轮次不是完成进度”的约束

## 3. 前端活动反馈

- [x] 3.1 增加可复用的 Agent 活跃度格式化逻辑，生成“已完成 N 轮”“第 N+1 轮进行中”和最近活动相对时间，并稳健处理无效或缺失时间
- [x] 3.2 在处理任务列表的语境研究行展示紧凑活动摘要，同时保留现有任务状态、阶段与进度布局
- [x] 3.3 在任务详情抽屉展示完整活动信息；字段缺失时隐藏整段，长时间未更新时不自动标记失败或卡死
- [x] 3.4 添加组件测试，覆盖轮次随轮询增长、进行中/已结束文案、详情同步、字段缺失、窄屏换行和无障碍状态播报不过度打扰

## 4. 验证与兼容性

- [x] 4.1 运行后端相关测试及完整前端单元测试，确认新增只读观测不会改变任务状态机、任务执行和既有 API 字段
- [x] 4.2 运行一个真实 Docker 语境研究任务，验证宿主 FastAPI 能在 OpenCode 写入 WAL 期间读到递增轮次和最新活动，且不需要 Docker socket、`docker exec` 或容器网络
- [x] 4.3 用数据库缺失、临时锁忙和不兼容 schema 进行故障注入，验证 `/tasks` 与 `/tasks/{id}` 仍成功且研究任务继续执行
- [x] 4.4 在桌面和移动端检查任务列表与详情，确认文案不呈现总轮次或百分比、无空占位且不与现有控件重叠
