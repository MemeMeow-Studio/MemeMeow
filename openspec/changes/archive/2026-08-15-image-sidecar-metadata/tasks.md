> **归档说明**：本 change 已被 `introduce-postgres-scoped-persistence` superseded-by。新 change 归档后，本 change 仅作为历史实现记录使用 `--skip-specs` 归档。

## 1. meme 语境元数据基础设施

- [x] 1.1 新增图片 sidecar 元数据服务，定义技术外层、`meme_context` schema、`pending/partial/ready/repair_required` 状态和字段来源。
- [x] 1.2 实现 `meme_context` schema 校验、字段长度/数量限制、未知字段保留、路径映射和图片 sha256 指纹校验。
- [x] 1.3 实现同目录临时文件加原子替换的读写逻辑，并覆盖损坏 JSON、版本不兼容、路径不匹配和写入中断。
- [x] 1.4 为元数据服务补充单元测试，验证 `图片完整文件名.json` 命名、同名不同扩展名隔离、无绝对路径泄露和不确定项不被提升为事实。

## 2. 图片生命周期集成

- [x] 2.1 修改图片上传流程：仅当图片与 `pending` sidecar 都成功提交时才报告成功；sidecar 写入失败时清理本次图片产物。
- [x] 2.2 修改图片重命名流程：同步迁移和更新 sidecar，遇到任一步失败时回滚到原图片和原 sidecar。
- [x] 2.3 在图片删除/清理路径中接入 sidecar 删除，并实现孤立 sidecar 与图片/指纹不匹配的识别和安全修复策略。
- [x] 2.4 扩展图片库响应，公开每张图片的语义状态及可安全展示的基础字段，不暴露绝对路径、模型密钥或内部错误细节。
- [x] 2.5 为上传、重命名、冲突、元数据写入失败和孤立文件修复补充 API 测试。

## 3. 视觉标注与研究结果持久化

- [x] 3.1 将单张视觉标注结果映射为画面事实字段：摘要、主体、可见文字和关键词，并记录模型、时间和来源。
- [x] 3.2 保持 `references`、`meaning` 与 `source_urls` 的研究/人工确认边界；将未确认或可能过时的候选写入 `uncertainties`。
- [x] 3.3 调整自动命名流程，使采用的候选描述与图片新路径、对应语义字段和来源一起写入 sidecar。
- [x] 3.4 扩展批量任务以保存逐图成功/失败状态，并确保单张失败不丢弃其他图片已持久化的语境。
- [x] 3.5 为重复标注、视觉模型失败、人工内容保护、研究结果校验和批量部分失败补充测试。

## 4. 语义检索索引迁移

- [x] 4.1 实现从 `summary`、`subjects`、`visible_text`、已确认 `references`、`meaning` 和 `keywords` 构造稳定 `semantic_document` 的规则。
- [x] 4.2 在索引构造中硬性排除 `search_queries`、`uncertainties` 和 `source_urls`，并为 `pending`、`partial` 与待修复记录实现规定的回退和状态报告。
- [x] 4.3 升级检索缓存格式和版本，写入语义文本哈希、sidecar schema 版本和图片指纹；白名单字段或模型变化后使旧索引失效。
- [x] 4.4 保持缓存原子替换和刷新期间旧索引可用的行为，为字段白名单、排除规则、状态回退和模型切换补充测试。

## 5. 既有图片迁移与运维接口

- [x] 5.1 提供幂等的元数据初始化/修复后台任务，为现有图片补齐最小 `pending` sidecar，且默认不调用视觉模型或外部研究服务。
- [x] 5.2 通过任务接口暴露迁移进度、成功数、失败项及各语义状态数量；更新保护模式端点白名单和 API 文档。
- [x] 5.3 为大批量目录、重复执行、任务中断、修复后索引重建和旧 v2 缓存失效补充集成测试。

## 6. 验收与文档

- [x] 6.1 更新 API 文档和 OpenAPI 描述，说明 sidecar 的 schema、字段证据边界、语义状态、索引字段白名单和重建条件。
- [x] 6.2 运行完整后端测试套件，并人工验证上传、视觉标注、研究结果写入、重命名、迁移、缓存重建和服务重启后的数据一致性。

## 7. Agent 标题与自动命名

- [x] 7.1 在研究输出和 sidecar schema 中加入可空 `title`，记录 Agent 来源，并把非空标题加入 embedding 白名单。
- [x] 7.2 调整视觉标注和上传自动命名流程：持久化自然语言标题，仅在显式启用时从标题派生安全文件名，标题更新不隐式改路径。
- [x] 7.3 更新 API 文档并补充标题校验、语义文本、自动命名和路径冲突测试，运行完整验收。
